const db = require("../../db/models/index");
const User = db.User;
const Milestone = db.Milestone;

export default async function dateControl(req, res) {
  const { method, body } = req;
  switch (method) {
    case "POST":
      break;
    case "PUT":
      break;
    case "GET":
      break;
    case "POST":
      break;

    default:
      break;
  }
}
